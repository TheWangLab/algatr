# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    paths:
      - "R/**.R"
      - "vignettes/**.Rmd"
      - .github/workflows/pkgdown.yaml
      - renv.lock
      - inst/CITATION
      - README.md
      - _pkgdown.yml
      - man/*
      - man/figures/*
      - Dockerfile
    branches: [ "main" ]
name: pkgdown

jobs:
  pkgdown:
    runs-on: ubuntu-latest
    concurrency:
      group: pkgdown-${{ github.event_name != 'pull_request' || github.run_id }}
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      # Pin R user lib so every step uses the same place
      R_LIBS_USER: /usr/local/lib/R/site-library
    permissions:
      contents: write
    container:
      image: ghcr.io/thewanglab/algatr:latest

    steps:
      - uses: actions/checkout@v4

      - name: System deps for source builds
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            build-essential gfortran libcurl4-openssl-dev libssl-dev libxml2-dev \
            libbz2-dev liblzma-dev rsync gdebi-core
          # sanity: create user lib if missing
          mkdir -p "$R_LIBS_USER"

      - name: Setup Pandoc
        uses: r-lib/actions/setup-pandoc@v2
        
      - name: Install pkgdown + devtools
        shell: Rscript {0}
        run: |
          install.packages(c("pkgdown","devtools"))

      - name: Install algatr (from source)
        shell: Rscript {0}
        run: |
          devtools::install(dependencies = FALSE)  # you install deps yourself next

      - name: Install deps via helper
        shell: Rscript {0}
        run: |
          library(algatr)
          alazygatr_packages()

      - name: Build site
        shell: Rscript {0}
        run: |
          pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)

      - name: Deploy to GitHub pages 🚀
        if: github.event_name != 'pull_request'
        uses: JamesIves/github-pages-deploy-action@v4.4.1
        with:
          clean: false
          branch: gh-pages
          folder: docs
