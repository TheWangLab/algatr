# Scale three layers of environmental data to R, G, and B for mapping

Scale three layers of environmental data to R, G, and B for mapping

## Usage

``` r
scaleRGB(env)
```

## Arguments

- env:

  SpatRaster or Raster\* with three layers

## Value

RGB-scaled values
