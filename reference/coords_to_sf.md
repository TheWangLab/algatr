# Convert from matrix, data frame, or sf to sf (sf is a pass through)

Convert from matrix, data frame, or sf to sf (sf is a pass through)

## Usage

``` r
coords_to_sf(coords)
```

## Arguments

- coords:

  sf object, data frame, or matrix representing coordinates

## Value

converted coords in sf format
