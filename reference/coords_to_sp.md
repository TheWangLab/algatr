# Convert from matrix, data frame, or sf to formatted sp

Convert from matrix, data frame, or sf to formatted sp

## Usage

``` r
coords_to_sp(coords)
```

## Arguments

- coords:

  sf object, data frame, or matrix representing coordinates

## Value

converted coords in sp format
