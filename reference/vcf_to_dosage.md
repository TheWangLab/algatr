# Convert a vcf to a dosage matrix

Convert a vcf to a dosage matrix

## Usage

``` r
vcf_to_dosage(x)
```

## Arguments

- x:

  can either be an object of class `vcfR` or a path to a .vcf file

## Value

dosage matrix
